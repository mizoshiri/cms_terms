<%= simple_form_for(@page) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.text_field :title %>
    <%= f.input :content %>
    <%= f.input :status %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
