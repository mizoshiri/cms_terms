<%= simple_form_for(@note) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.label :title %>
    <%= f.text_field :title, :required => true, :autofocus => true %>
    <%= f.input :content %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
